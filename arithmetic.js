//arithmetic.js
 const subtract=(a,b)=>a-b
 const Mul=(a,b)=>a*b
 const divide = (a, b) => a / b;
 const modulo = (a, b) => a % b;
 const sum=(a,b)=>a+b

//module added

module.exports={subtract, Mul, sum, divide, modulo}